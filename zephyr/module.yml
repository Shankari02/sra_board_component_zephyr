# West YAML for SRA board component

build:
  settings:
    # These may be removed if unused
    board_root: samples/mcpwm
    dts_root: .
    soc_root: .
    arch_root: .
  cmake: .
  kconfig: subsys/sra/Kconfig
samples:
  - samples
tests:
  - tests
